# Level 3 - Bad Quality
# Simulates a poor network with significant issues

# Delay settings
DELAY_BASE=300         # Base delay in milliseconds
DELAY_JITTER=100       # Jitter (±ms) around base delay
DELAY_CORRELATION=25   # Correlation percentage for delay

# Loss settings
LOSS_PERCENTAGE=1.5    # Packet loss percentage
LOSS_CORRELATION=20    # Correlation percentage for loss

# Corruption settings
CORRUPT_PERCENTAGE=0.1   # Packet corruption percentage

# Duplication settings
DUPLICATE_PERCENTAGE=0.5  # Packet duplication percentage

# Reordering settings
REORDER_GAP=6          # Check every N packets for reordering
REORDER_CHANCE=2       # Percentage chance of reordering
REORDER_CORRELATION=35 # Correlation percentage for reordering

# Bandwidth settings
BANDWIDTH_LIMIT="10mbit"  # Maximum bandwidth
