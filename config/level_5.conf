# Level 5 - Almost Not Usable
# Simulates an extremely poor network, barely functional

# Delay settings
DELAY_BASE=800         # Base delay in milliseconds
DELAY_JITTER=400       # Jitter (±ms) around base delay
DELAY_CORRELATION=35   # Correlation percentage for delay

# Loss settings
LOSS_PERCENTAGE=5      # Packet loss percentage
LOSS_CORRELATION=30    # Correlation percentage for loss

# Corruption settings
CORRUPT_PERCENTAGE=0.5   # Packet corruption percentage

# Duplication settings
DUPLICATE_PERCENTAGE=2   # Packet duplication percentage

# Reordering settings
REORDER_GAP=4          # Check every N packets for reordering
REORDER_CHANCE=5       # Percentage chance of reordering
REORDER_CORRELATION=50 # Correlation percentage for reordering

# Bandwidth settings
BANDWIDTH_LIMIT="2mbit"   # Maximum bandwidth
