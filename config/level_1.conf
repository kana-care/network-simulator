# Level 1 - Slightly Reduced Quality
# Simulates a good network with minor impairments

# Delay settings
DELAY_BASE=50          # Base delay in milliseconds
DELAY_JITTER=20        # Jitter (±ms) around base delay
DELAY_CORRELATION=15   # Correlation percentage for delay

# Loss settings
LOSS_PERCENTAGE=0.1    # Packet loss percentage
LOSS_CORRELATION=10    # Correlation percentage for loss

# Corruption settings
CORRUPT_PERCENTAGE=0.01  # Packet corruption percentage

# Duplication settings
DUPLICATE_PERCENTAGE=0.1  # Packet duplication percentage

# Reordering settings
REORDER_GAP=10         # Check every N packets for reordering
REORDER_CHANCE=0.5     # Percentage chance of reordering
REORDER_CORRELATION=25 # Correlation percentage for reordering

# Bandwidth settings
BANDWIDTH_LIMIT="50mbit"  # Maximum bandwidth (use "unlimited" for no limit)
