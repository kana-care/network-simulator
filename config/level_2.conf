# Level 2 - Quite Bad Quality
# Simulates a degraded network with noticeable issues

# Delay settings
DELAY_BASE=150         # Base delay in milliseconds
DELAY_JITTER=50        # Jitter (±ms) around base delay
DELAY_CORRELATION=20   # Correlation percentage for delay

# Loss settings
LOSS_PERCENTAGE=0.5    # Packet loss percentage
LOSS_CORRELATION=15    # Correlation percentage for loss

# Corruption settings
CORRUPT_PERCENTAGE=0.05  # Packet corruption percentage

# Duplication settings
DUPLICATE_PERCENTAGE=0.3  # Packet duplication percentage

# Reordering settings
REORDER_GAP=8          # Check every N packets for reordering
REORDER_CHANCE=1       # Percentage chance of reordering
REORDER_CORRELATION=30 # Correlation percentage for reordering

# Bandwidth settings
BANDWIDTH_LIMIT="20mbit"  # Maximum bandwidth
