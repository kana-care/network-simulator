# Level 0 - Normal Network (Disable Simulation)
# This configuration disables all network simulation

# Delay settings (not used for level 0)
DELAY_BASE=0
DELAY_JITTER=0
DELAY_CORRELATION=0

# Loss settings (not used for level 0)
LOSS_PERCENTAGE=0
LOSS_CORRELATION=0

# Corruption settings (not used for level 0)
CORRUPT_PERCENTAGE=0

# Duplication settings (not used for level 0)
DUPLICATE_PERCENTAGE=0

# Reordering settings (not used for level 0)
REORDER_GAP=0
REORDER_CHANCE=0
REORDER_CORRELATION=0

# Bandwidth settings (not used for level 0)
BANDWIDTH_LIMIT="unlimited"
